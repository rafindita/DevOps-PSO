# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy container app to Azure Web App - scholarseek-app-7670

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read #This is required for actions/checkout
      packages: write #This is required to push to ghcr.io

    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Log in to container registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push container image to registry
        uses: docker/build-push-action@v3
        with:
          context: .
          push: true
          tags: ghcr.io/rafindita/devops-pso:${{ github.sha }}
          file: ./Dockerfile

#   deploy:
#     runs-on: ubuntu-latest
#     permissions:
#       id-token: write #This is required for requesting the JWT
#       contents: read #This is required for actions/checkout
# 
#     needs: build
#     
#     steps:
#       
#       - name: Login to Azure
#         uses: azure/login@v2
#         with:
#           client-id: ${{ secrets.AZUREAPPSERVICE_CLIENTID_E8E9B0FF7D9145119136CD4C5F9E830A }}
#           tenant-id: ${{ secrets.AZUREAPPSERVICE_TENANTID_FAFB86707A7D4AC1837A80AA08354B01 }}
#           subscription-id: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_AC236E5BA7E94009B12CBC69C4951052 }}
# 
#       - name: Deploy to Azure Web App
#         id: deploy-to-webapp
#         uses: azure/webapps-deploy@v2
#         with:
#           app-name: 'scholarseek-app-7670'
#           slot-name: 'Production'
#           images: 'ghcr.io/rafindita/devops-pso:${{ github.sha }}'
